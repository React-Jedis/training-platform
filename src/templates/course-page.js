import React from 'react';
import { graphql } from 'gatsby';

const CoursePage = ({ data }) => {
  return (
    <div className="p-4 text-gray-200">
      <div className="mt-5 p-5 rounded-md bg-corporative-5"></div>
    </div>
  );
};

export default CoursePage;